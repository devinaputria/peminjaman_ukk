import 'package:flutter/material.dart';

class AppColors {
  static const Color sidebar = Color(0xFF2A5191);
  static const Color navbar = Color(0xFFFFF8E1);
  static const Color icon = Color(0xFF01386C);
  static const Color background = Color(0xFFFFF8E1);

  // WARNA UTAMA UNTUK UI KONSISTEN
  static const Color primary = Color(0xFF2A5191);   // bisa pakai sama dengan sidebar
  static const Color onPrimary = Colors.white;       // teks/icon di atas primary
}
